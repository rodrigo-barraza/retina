export const metadata = {
  title: "VRAM Benchmark",
  description:
    "GPU memory profiling for locally-served models — measured vs. estimated VRAM consumption.",
};

export default function VramBenchmarkLayout({ children }) {
  return children;
}
